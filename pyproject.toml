[project]
name = "quiz-bot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.122.0",
]

[dependency-groups]
dev = [
    "ruff>=0.14.5",
]
